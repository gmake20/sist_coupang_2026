<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="org.doit.goodpang.mapper.ProductOptionMapper">

  <!-- List<ProductOptionDTO> selectOptionByProductNo(int productNo); -->
  <select id="selectOptionByProductNo" parameterType="int" resultType="org.doit.goodpang.domain.ProductOptionDTO">
  
   	SELECT
         OPTION_ID,
         OPTION1_TYPE,
         OPTION1_VALUE,
         OPTION2_TYPE,
         OPTION2_VALUE,
         OPTION3_TYPE,
         OPTION3_VALUE,
         NVL(PRICE, 0) AS PRICE,
         NORMAL_PRICE,
         QUANTITY,
         STATUS
    FROM PRODUCT_OPTION
    WHERE PRODUCT_NO = #{productNo}
    ORDER BY OPTION_ID
  
  </select>

<!-- 	ProductOptionDTO selectOptionById(int optionId); -->
  <select id="selectOptionById" parameterType="int" resultType="org.doit.goodpang.domain.ProductOptionDTO">
     
   	SELECT
          OPTION_ID,
          OPTION1_TYPE,
          OPTION1_VALUE,
          OPTION2_TYPE,
          OPTION2_VALUE,
          OPTION3_TYPE,
          OPTION3_VALUE,
          NVL(PRICE, 0) AS PRICE,
          NORMAL_PRICE,
          QUANTITY,
          STATUS
     FROM PRODUCT_OPTION
     WHERE OPTION_ID = #{optionId}
  
  </select>

 
	
  </mapper>