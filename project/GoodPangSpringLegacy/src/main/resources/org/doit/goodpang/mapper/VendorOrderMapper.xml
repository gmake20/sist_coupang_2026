<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

  <mapper namespace="org.doit.goodpang.mapper.VendorOrderMapper">

	<!--
	  findBySellerNo/countBySellerNo가 공유하는 검색 필터 (기존 VendorOrderListDAO.appendFilters).
	  orderStatus / deliveryStatus / paymentStatus 세 필터는 모두 같은 컬럼(ORDER_STATUS)을 다른 관점으로 본다.
	    orderStatus:    결제완료 / 배송중 / 배송완료 / 주문취소 (ORDER_STATUS 값 그대로)
	    deliveryStatus: 출고대기(=결제완료) / 배송중 / 배송완료
	    paymentStatus:  결제완료(=주문취소 아님) / 결제취소(=주문취소)
	-->
	<sql id="orderFilters">
            <if test="startDate != null">
              AND TRUNC(O.ORDER_DATE) &gt;= #{startDate}
            </if>
            <if test="endDate != null">
              AND TRUNC(O.ORDER_DATE) &lt;= #{endDate}
            </if>
            <if test="orderStatus != null and orderStatus.trim() != ''">
              AND O.ORDER_STATUS = #{orderStatus}
            </if>
            <if test="deliveryStatus != null and deliveryStatus.trim() != ''">
              <choose>
                <when test="deliveryStatus == '출고대기'">
              AND O.ORDER_STATUS = '결제완료'
                </when>
                <otherwise>
              AND O.ORDER_STATUS = #{deliveryStatus}
                </otherwise>
              </choose>
            </if>
            <choose>
              <when test="paymentStatus == '결제완료'">
              AND O.ORDER_STATUS != '주문취소'
              </when>
              <when test="paymentStatus == '결제취소'">
              AND O.ORDER_STATUS = '주문취소'
              </when>
            </choose>
	</sql>

	<!-- public List<VendorOrderListDTO> findBySellerNo(int sellerNo, Date startDate, Date endDate, String orderStatus, String deliveryStatus, String paymentStatus, int offset, int pageSize); -->
	<!-- OPTION_LABEL: 비어있지 않은 옵션값만 " / "로 이어붙임 (기존 DAO의 buildOptionLabel) -->
	<select id="findBySellerNo" resultType="org.doit.goodpang.domain.VendorOrderListDTO">
            SELECT
                O.ORDER_NO,
                OD.ORDER_DETAIL_NO,
                P.PRODUCT_NAME,
                SUBSTR(
                    NVL2(PO.OPTION1_VALUE, ' / ' || PO.OPTION1_VALUE, NULL)
                    || NVL2(PO.OPTION2_VALUE, ' / ' || PO.OPTION2_VALUE, NULL)
                    || NVL2(PO.OPTION3_VALUE, ' / ' || PO.OPTION3_VALUE, NULL)
                , 4) AS OPTION_LABEL,
                OD.ORDER_QTY,
                OD.PRICE,
                O.ORDER_STATUS,
                O.ORDER_DATE,
                M.MEMBER_NAME AS BUYER_NAME,
                M.PHONE       AS BUYER_PHONE,
                IMG.IMAGE_URL AS THUMBNAIL_URL
            FROM ORDER_DETAIL OD
                JOIN ORDERS O ON OD.ORDER_NO = O.ORDER_NO
                JOIN PRODUCT P ON OD.PRODUCT_NO = P.PRODUCT_NO
                JOIN MEMBER M ON O.MEMBER_NO = M.MEMBER_NO
                LEFT JOIN PRODUCT_OPTION PO ON OD.OPTION_ID = PO.OPTION_ID
                LEFT JOIN (
                    SELECT PRODUCT_NO, IMAGE_URL,
                           ROW_NUMBER() OVER (PARTITION BY PRODUCT_NO ORDER BY OPTION_ID, IMAGE_ORDER) AS RN
                    FROM PRODUCT_IMAGE
                    WHERE IMAGE_PURPOSE = '대표'
                ) IMG ON IMG.PRODUCT_NO = P.PRODUCT_NO AND IMG.RN = 1
            WHERE P.SELLER_NO = #{sellerNo}
            <include refid="orderFilters" />
            ORDER BY O.ORDER_DATE DESC, O.ORDER_NO DESC
            OFFSET #{offset} ROWS FETCH NEXT #{pageSize} ROWS ONLY
	</select>

	<!-- public int countBySellerNo(int sellerNo, Date startDate, Date endDate, String orderStatus, String deliveryStatus, String paymentStatus); -->
	<select id="countBySellerNo" resultType="int">
            SELECT COUNT(*)
            FROM ORDER_DETAIL OD
                JOIN ORDERS O ON OD.ORDER_NO = O.ORDER_NO
                JOIN PRODUCT P ON OD.PRODUCT_NO = P.PRODUCT_NO
            WHERE P.SELLER_NO = #{sellerNo}
            <include refid="orderFilters" />
	</select>

	<!-- public VendorOrderStatSummaryDTO countStats(int sellerNo); -->
	<!-- 상단 통계 카드 - DELIVERY_END_DATE 기준으로 "오늘" 배송완료 건수를 센다 -->
	<select id="countStats" resultType="org.doit.goodpang.domain.VendorOrderStatSummaryDTO">
            SELECT
                COUNT(DISTINCT CASE WHEN O.ORDER_STATUS = '결제완료' THEN O.ORDER_NO END) AS WAITING_COUNT,
                COUNT(DISTINCT CASE WHEN O.ORDER_STATUS = '배송중' THEN O.ORDER_NO END) AS SHIPPING_COUNT,
                COUNT(DISTINCT CASE WHEN O.ORDER_STATUS = '배송완료' THEN O.ORDER_NO END) AS DELIVERED_COUNT,
                COUNT(DISTINCT CASE WHEN O.ORDER_STATUS = '배송완료'
                                      AND D.DELIVERY_END_DATE IS NOT NULL
                                      AND TRUNC(D.DELIVERY_END_DATE) = TRUNC(SYSDATE)
                                 THEN O.ORDER_NO END) AS DELIVERED_TODAY_COUNT
            FROM ORDERS O
                JOIN ORDER_DETAIL OD ON OD.ORDER_NO = O.ORDER_NO
                JOIN PRODUCT P ON OD.PRODUCT_NO = P.PRODUCT_NO
                LEFT JOIN DELIVERY D ON D.ORDER_NO = O.ORDER_NO
            WHERE P.SELLER_NO = #{sellerNo}
	</select>

  </mapper>
