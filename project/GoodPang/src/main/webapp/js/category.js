// 카테고리 목록 페이지(category_list.jsp) 전용 JS.
//
// 정렬 / 페이지네이션 / 카테고리·평점·가격 필터는 전부 <a href="...">로 페이지 전체를 다시 요청하는
// 방식임 (원본 coupang.com 도 실제로 이렇게 동작함 — SPA 부분갱신 아님, ref/category/STRUCTURE.md
// 10장 참고). 그래서 지금은 JS 로 할 일이 없음.
//
// 나중에 색상 등 "체크박스형" 필터를 실제로 동작시키게 되면(지금은 화면만 있고 클릭해도 안 움직임),
// 그때 클릭 시 선택된 값들을 모아서 category.jsp 폼을 만들거나 location.href 를 직접 조립하는
// 코드가 여기 들어가면 됨.
